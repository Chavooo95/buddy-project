

framework:
  secret: '%env(APP_SECRET)%'
  session: true
  http_method_override: false
  handle_all_throwables: true
  php_errors:
    log: true

when@test:
  framework:
    test: true

when@test_orm:
  framework:
    test: true